# JLPT 标准卷面格式规范 v1

纯文本，UTF-8。行首 `#` 为标记行，其余行为内容行。所有日文原文必须逐字保真，不得意译、省略或"修复"原文。

## 标记

```
#卷 2024-12 N1          ← 卷号（年-月）+ 级别，每个文件第一行
#科 言語知識            ← 言語知識 / 読解 / 聴解 三选一，每个文件一个科目
#大題 問題1             ← 大题号，照抄试卷（問題1、問題2…）
#題 3                   ← 小题号（全卷连续编号，照抄试卷）
#干 ……                 ← 题干。考察点用＜＞括出（下划线/加粗词）。填空写（　）
#选 1 ……               ← 选项，编号+空格+内容。即時応答只有 3 个选项属正常
#答 2                   ← 官方答案编号。答案未知时省略此行
#文 A1                  ← 文章块开始，块名自定（A1、B2…），下一行起为正文
#文完                   ← 文章块结束
#題 46 @文 A1           ← 题目引用文章块
```

## 细则

1. **文法组句题（★题）**：题干中四个空写作 `＿＿ ＿＿ ＿★＿ ＿＿`，★位置照抄试卷。
2. **文章语法题（cloze）**：文章作 `#文` 块，空位在正文中写作 `【41】`、【42】…（编号=题号），题目行 `#題 41 @文 C1`，`#干` 行可省。
3. **読解**：每篇文章一个 `#文` 块，正文保留段落空行。文章后注（注1）…照抄在正文末尾。
4. **聴解**：每题的听力原文（会話/スクリプト）作该题的 `#文` 块；试卷上印刷的选项进 `#选`；試卷上没印选项的（概要理解、即時応答），选项内容从听力原文里取。設問句放 `#干`。
5. 转写时无法确认的字用 `〔?〕` 紧跟其后标记。
6. 每套卷输出 3 个文件：`<卷号>_言語知識.txt`、`<卷号>_読解.txt`、`<卷号>_聴解.txt`。

## 示例

```
#卷 2024-12 N1
#科 言語知識
#大題 問題1
#題 1
#干 事件の＜真相＞を究明する。
#选 1 しんそう
#选 2 しんしょう
#选 3 まそう
#选 4 まっしょう
#答 1
```
